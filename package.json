{
  "name": "dingo-den",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @dingo-den/* dev",
    "build": "pnpm --filter @dingo-den/* build",
    "preview": "pnpm --filter @dingo-den/* preview",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
